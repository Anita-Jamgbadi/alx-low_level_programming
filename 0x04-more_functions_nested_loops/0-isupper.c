#include "main.h"

/**
 * _isupper - checks if a character is uppercase
 * Return: 1 if true, 0 if false
 */

int _isupper(int c)
{
	int value;

	if (c <= 'A' && c >= 'Z')
	{
		value = 1;
	}
	else
	{
		value = 0;
	}
	return (value)
}
