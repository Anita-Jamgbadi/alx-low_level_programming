#include <stdio.h>

/**
 * main - prints its name followed by a new line
 * @argc: counter
 * @argv: array of arguments
 * Return: EXIT_SUCCESS
 */

int main(int argc, char *argv[])
{
	printf("./%s\n", argc);
	return (EXIT_SUCCESS);
}
