#include "main.h"
#include <stdio.h>

/**
 * main - Prints out the number of arguments passed to it
 * @argc: counter
 * @argv: argument vector
 * Return: EXIT_SUCCESS
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (EXIT_SUCCESS);
}
